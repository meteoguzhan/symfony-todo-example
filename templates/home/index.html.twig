<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Developer and Task Lists</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .list-group-flush {
            max-height: 400px;
            overflow-y: auto;
        }
    </style>
</head>
<body class="container mt-5">
<h2 class="text-primary mb-4">Developer and Task Lists</h2>
<div class="row">
    {% for item in items.assignments %}
        <div class="col-md-4 mb-4">
            <div class="card">
                <div class="card-header">
                    <div class="d-flex justify-content-between">
                        <h3 class="mb-0">{{ item[0].developerName }}'s Tasks</h3>
                        <h3 class="mb-0"><span class="badge badge-primary">{{ items.totalWeekPerDeveloper[item[0].developerId] }} Weeks</span></h3>
                    </div>
                </div>
                <ul class="list-group list-group-flush">
                    {% for task in item %}
                        <li class="list-group-item">
                            <div class="d-flex justify-content-between align-items-center">
                                <span>{{ task.taskName }}</span>
                                <span class="badge badge-primary">{{ task.duration }} hours</span>
                            </div>
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    {% endfor %}
</div>
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.1/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
